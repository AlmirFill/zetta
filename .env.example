# Exemplo de variáveis de ambiente para deploy
# Usar DATABASE_URL (preferível) ou as variáveis separadas abaixo

# DATABASE_URL (formato MySQL)
# Ex: mysql://usuario:senha@host:3306/nome_do_banco
DATABASE_URL=

# OU variáveis separadas
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=gestao_bovina

# Porta do app (Render fornece automaticamente a PORT)
PORT=3000
